{
  "name": "@bot/worker",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "node ../../scripts/dev-watch.mjs src/index.ts",
    "start": "tsx src/index.ts",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@bot/adapter-kit": "workspace:*",
    "@bot/adapters": "workspace:*",
    "@bot/core": "workspace:*",
    "@bot/db": "workspace:*",
    "@bot/logging": "workspace:*",
    "@bot/memory": "workspace:*"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
