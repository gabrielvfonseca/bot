---
type Props = {
  href?: string;
  label?: string;
};

const { href = "/", label = "Bot home" } = Astro.props as Props;
---

<a class="brand-lockup" href={href} aria-label={label}>
  <img
    class="brand-lockup__mark"
    src="/brand/bot-mark.svg"
    width="36"
    height="36"
    alt=""
    aria-hidden="true"
  />
  <span class="brand-lockup__word">bot</span>
</a>
