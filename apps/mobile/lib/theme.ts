/** Product palette for Expo. Prefer these over hardcoded Bot hexes.
 * System chrome (nav bars, fills, labels) stays in `./native` via PlatformColor. */
export { botColors, tokens } from "@bot/ui-tokens";
